import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Image Compressor",
  "Reduce image file size by customizing quality, scaling dimensions, and converting formats client-side instantly with complete data privacy."
);
