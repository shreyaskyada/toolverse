import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata(
  "Certificate Generator",
  "Design and generate custom certificates of achievement instantly. Enter the recipient, course, and date, and download as a high-quality image completely client-side."
);
